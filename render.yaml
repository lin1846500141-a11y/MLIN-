# Render 部署蓝图(Blueprint)
# 在 https://render.com 选 New -> Blueprint,连这个仓库即可自动读取此配置
services:
  - type: web
    name: miku-wiki
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
